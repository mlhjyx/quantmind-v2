# 灾备演练 Disaster Drill — Round 1 (2026-05-14, HC-2c)

> **本文件 = V3 横切层 Plan v0.3 §A HC-2c deliverable** — V3 §14.1 灾备演练 ≥1 round
> synthetic injection sediment. Round 1 覆盖 V3 §14 失败模式 mode 1-12.
>
> **Methodology**: synthetic injection (pytest, instant, 0 wall-clock wait) — 沿用
> TB-5a synthetic scenario fixture 体例 + memory feedback_no_observation_periods 反
> 日历式观察期. **NOT** V3 §14.1 line 1465 "每月 1 次" wall-clock drill — V3 §14.1
> production cadence 建议设计层保留, Gate D closure 验收走 synthetic injection ≥1
> round (Plan v0.3 §G push-back #3 决议).
>
> **Drill file**: `backend/tests/test_v3_hc_2c_disaster_drill.py` (29 tests, CI green).
> **Enforcement matrix cross-ref**: `docs/audit/v3_hc_2a_failure_mode_enforcement_matrix_2026_05_14.md` (HC-2a).

---

## Run metadata

- **Round**: 1
- **Date**: 2026-05-14 (Session 53+29, HC-2c)
- **Modes covered**: 12 (V3 §14 mode 1-12 — drill scope per Plan v0.3 §A HC-2c)
- **Drill file**: `backend/tests/test_v3_hc_2c_disaster_drill.py`
- **Test count**: 29 (CI green, ruff clean)
- **Wall clock**: ~0.07s (synthetic injection — 0 wall-clock wait)
- **Real-system contact**: 0 (全 PURE rule / mock conn / fixture — 0 broker / 0 .env / 0 真 live system)
- **HC-2 enforcement surface exercised**: HC-1 (5 polled meta-alert rules) + HC-2b2 (G7 broker stuck / G8 Redis auto-reconnect) + HC-2b3 (G3 PG health / G4 Crisis regime)

---

## §1 Per-mode round results

每 mode round (§D SOP): 注入 trigger condition → assert 检测 fire → assert 降级路径 →
assert 恢复条件 → assert 元告警 flag (若 wired).

| mode | 失败模式 | round 结果 | 检测 | 降级 | 恢复 | 元告警 | enforcement source |
|---|---|---|---|---|---|---|---|
| 1 | LiteLLM cloud 全挂 | ✅ PASS | `evaluate_litellm_failure_rate` 失败率 80% > 50% → triggered | Ollama fallback (router) | 失败率回落 → not triggered | ✅ P0 wired | HC-1 |
| 2 | xtquant subscribe_quote 断连 | 🟡 PARTIAL | `evaluate_l1_heartbeat` stale 400s > 300s → triggered (PURE rule ✅) | — | fresh tick → not triggered | 🟡 rule wired, collector no-signal | HC-1 rule / **G2 DEFER** |
| 3 | PG OOM / lock | ✅ PASS | `evaluate_pg_health` idle-in-tx 60 > 50 → triggered | memory-cache full degrade **DEFER** (G3) | idle-in-tx drains → not triggered | ✅ P0 wired | HC-2b3 G3 |
| 4 | Redis 不可用 | ✅ PASS | `RedisThresholdCache._ensure_redis` | `InMemoryThresholdCache` fallback (get → None) | re-inject client → `_ensure_redis` True, **0 process restart** | ❌ MISSING (G8 — auto-reconnect only) | HC-2b2 G8 |
| 5 | DingTalk webhook fail | ✅ PASS | `evaluate_dingtalk_push` no-200 → triggered | channel chain 主 DingTalk → 备 email → log-P0 | push 200 → not triggered | ✅ P0 wired | HC-1 / HC-1b2 |
| 6 | News 6 源全 timeout | ✅ PASS | `evaluate_news_sources_timeout` 6/6 timeout → triggered | fail-open (alert 仍发) | 任 1 源恢复 (5/6) → not triggered | ✅ P1 wired | HC-1 |
| 7 | Tushare API 限速 | 🟡 PARTIAL | inline `is_rate_limit` keyword-match (`tushare_api.py:107-109`) | rate-limit → 60s 固定冷却 / 非 → 指数退避 | retry loop re-attempt | ❌ MISSING (G10, P2) | inline / **G10 DEFER** |
| 8 | user 离线 + STAGED 30min timeout | ✅ PASS | `check_timeout` past 30min window | STAGED default 执行 → `TIMEOUT_EXECUTED` (反向决策权) | user 上线 → `cancel` | ❌ 不元告警 (设计行为); >35min 失效变体 → `evaluate_staged_overdue` P0 | HC-1 / L4 planner |
| 9 | 千股跌停极端 regime | ✅ PASS | `evaluate_market_crisis` 大盘 -8% / 跌停 600 → triggered | `BatchedPlanner` 分 3 批平仓 | regime 恢复 → not triggered | ✅ P0 wired | HC-2b3 G4 |
| 10 | 误触发 (高 false positive) | 🟡 PARTIAL | — (no quantitative 误报率 threshold) | RiskReflector V4-Pro 定性 5-维反思 (qualitative path importable) | weekly reflection cycle | ❌ MISSING | **G12 DEFER** |
| 11 | RealtimeRiskEngine crash | 🟡 PARTIAL | `evaluate_l1_heartbeat` post-crash stale → triggered (PURE rule ✅) | — | restart success | 🟡 rule wired, collector no-signal | HC-1 rule / **G1 DEFER** |
| 12 | broker_qmt 接口故障 | ✅ PASS | `_sweep_stuck_inner` SELECT stuck CONFIRMED/TIMEOUT_EXECUTED > 5min | retry `execute_plan`; 仍卡 → 汇总 emit | broker 恢复 → retry resolves | ✅ P0 wired (`BROKER_PLAN_STUCK` event-emitted) | HC-2b2 G7 |

**Summary**: 8/12 ✅ PASS (完整 检测→降级→恢复→元告警 chain verified) / 4/12 🟡 PARTIAL
(检测 PURE rule verified, 但有 carried gap — 见 §2).

---

## §2 Gap findings (drill round REVEALED — sustained HC-2a matrix §3)

灾备演练的价值 = REVEAL gap, 非 silent pass. Round 1 confirmed 4 carried gaps:

| # | mode | gap | HC-2a matrix ref | carried 去向 |
|---|---|---|---|---|
| D1 | mode 2 | xtquant 断连 collector no-signal — `_collect_l1_heartbeat` hard-wired `last_tick_at=None`; PURE rule `evaluate_l1_heartbeat` 工作正常但无 production XtQuantTickSubscriber runner instrument | G2 | **Plan v0.4 cutover** (production realtime runner wiring) |
| D2 | mode 11 | RealtimeRiskEngine crash — 同 mode 2 根因 (no production realtime runner / no Servy heartbeat for it); PURE rule 覆盖 post-crash heartbeat-stale | G1 | **Plan v0.4 cutover** (shared root cause with D1) |
| D3 | mode 7 | Tushare 限速 元告警 MISSING — 检测 (inline `is_rate_limit`) + 降级 (60s 冷却 / 指数退避) EXISTS, 但无 Tushare rate-limit meta-alert rule | G10 (spec ⚠️ P2) | future sprint (P2 低优先) |
| D4 | mode 10 | 误触发 无 quantitative 误报率 threshold — `meta_alert_rules` 0 `evaluate_false_positive_*`; RiskReflector 走 V4-Pro 定性反思非量化触发 | G12 (spec ⚠️ P2) | future sprint (量化 threshold 是否必要 = scope decision) |

**真值**: mode 3 (PG memory-cache full degrade) 也是 carried gap (HC-2a G3) — 但 mode 3
的 **检测 + 元告警 chain 已 PASS** (HC-2b3 G3), 仅 full-degrade path carried; 故 mode 3
列 ✅ PASS (chain verified) 而非 🟡 PARTIAL. mode 9 §14.2 Crisis Mode 行为同理 carried
(HC-2b3 G4 scope) 但 mode 9 检测+降级+元告警 chain 已 PASS.

---

## §3 Cumulative HC-2 enforcement verification

Round 1 drill 端到端 verified HC-2 sprint 累积 enforcement (a/b/b2/b3 → c):

- **HC-1** (5 polled meta-alert rules): mode 1 / 2 / 5 / 6 / 8 — `evaluate_litellm_failure_rate`
  / `evaluate_l1_heartbeat` / `evaluate_dingtalk_push` / `evaluate_news_sources_timeout`
  / `evaluate_staged_overdue` 全 synthetic injection PASS.
- **HC-2b2** (G7 broker stuck / G8 Redis auto-reconnect): mode 12 / 4 — `_sweep_stuck_inner`
  retry + `BROKER_PLAN_STUCK` 元告警 PASS; `RedisThresholdCache` auto-reconnect 0-restart PASS.
- **HC-2b3** (G3 PG health / G4 Crisis regime): mode 3 / 9 — `evaluate_pg_health` +
  `evaluate_market_crisis` + `BatchedPlanner` degrade 全 PASS.

注: HC-2b (G5 RiskReflector 失败 / G6 LIVE_TRADING_DISABLED 双锁) = V3 §14 mode 14 / 15
— drill scope 是 mode 1-12 (Plan v0.3 §A HC-2c), mode 13-15 不在 round 1 演练清单
(V3 §14.1 line 1465 "模拟 failure mode 1-12"). G5/G6 enforcement deep coverage 在各自
test file (HC-2b PR #354).

---

## §4 Cumulative cite footer

- **HC-2c deliverable**: 本 sediment doc + `backend/tests/test_v3_hc_2c_disaster_drill.py`
  (29 tests) + `docs/risk_reflections/README.md` disaster_drill/ dir 体例补充 + ADR-074
- **HC-2 chunked 5 sub-PR closure**: HC-2a (15-mode enforcement matrix + gap list) →
  HC-2b (G5/G6) → HC-2b2 (G7/G8) → HC-2b3 (G3/G4) → HC-2c (本 — 灾备演练 ≥1 round + ADR-074)
- **关联**: V3 §14 失败模式表 line 1445-1461 / §14.1 灾备演练 line 1463-1472 / §14.2
  Crisis Mode line 1474-1483 / HC-2a matrix (enforcement matrix + gap list SSOT) /
  Plan v0.3 §A HC-2c + §D 真测期 SOP / ADR-074 (HC-2 closure) / 铁律 28 (发现即报告 —
  4 gap finding) / memory feedback_no_observation_periods (反日历式观察期)
- **5/5 红线 sustained**: cash=￥993,520.66 / 0 持仓 / LIVE_TRADING_DISABLED=true /
  EXECUTION_MODE=paper / QMT_ACCOUNT_ID=81001102 — HC-2c = test + docs, 0 code change /
  0 broker / 0 .env / 0 DB mutation (drill 全走 PURE rule / mock conn / fixture)
