# ADR-DRAFT — Append-only candidate ADR backlog

> **意义**: 跨 sprint cumulative ADR # 候选 + 主题 sediment, 反**散落 LL backlog / sprint_state cite / handoff** 4 source 漂移 (沿用 LL-105 SOP-6 ADR # reservation 4 source cross-verify). **单 source 锁定 candidate** 给下一 sprint 起手 promote committed.
> **机制**: append-only — 每 sprint candidate 加一行, **0 删 0 改顺序**. promote 走时 row 标 `→ ADR-XXX (committed)` + cite source PR/file.
> **触发**: 5-07 sub-PR 8a (5-06 ADR-037 + 铁律 45 + LL-106 sediment 后**首次 SOP enforcement** 落 sub-PR 8a, drift catch case #7 候选 sediment).
> **关联文档**: [docs/adr/REGISTRY.md](REGISTRY.md) (committed + reserved SSOT) / [docs/adr/README.md](README.md) (索引 + 模板) / [LESSONS_LEARNED.md](../../LESSONS_LEARNED.md) LL-105/106 (4 source cross-verify + fresh read SOP).

## 候选 list (append-only, 0 delete 0 reorder)

| # | 主题 | source (sprint period sediment) | 状态 | promote target |
|---|---|---|---|---|
| 1 | News retention cron (hypertable + 90 day retention) | sub-PR 8a 5-07 sediment, V3§3.1 line 354 待办 + 沿用 docs/audit/risk_replay/ retention 体例 | candidate | sub-PR 10 起手时 promote |
| 2 | News fetch query strategy SSOT (5 源 vs RSSHub route path 体例分裂) | sub-PR 8a 5-07 sediment, V3§3.1 sub-PR 6 docstring "RSSHub 走独立 pipeline". **5-07 sub-PR 8b-cadence-A partial promote**: Beat schedule mechanism (Celery Beat 反 Task Scheduler) + cron 频率 (`crontab(hour="3,7,11,15,19,23", minute=0)` 4-hour offset 3h) + RSSHub 路由层契约 (standalone POST /api/news/ingest_rsshub) + cost cap decision sediment (沿用 row 6 待办) + dingtalk rate cap decision sediment (沿用 sub-PR 9 alert wiring 待办) + Risk v2 Beat indefinite paused 体例 **ADR-043** sediment ✅; **残余 sub-task**: News Beat schedule entry register (sub-PR 8b-cadence-B 待办) + RSSHub multi-route 503 fix (audit chunk C 待办) | **→ ADR-043 (committed, partial)** | 沿用 sub-PR 8b-cadence-B + audit chunk C 待办 完整 closure |
| 3 | Beat schedule paused indefinite 体例 (X9 reverse case sediment) | sub-PR 8a 5-07 verify finding P0-1, risk-daily-check + intraday-risk-check 4-29 PAUSE 7 天 + sub-PR 8a 决议 indefinite paused | candidate | S5 L1 实时化时 sunset 切换 / 或独立 ADR audit Week 2 batch |
| 4 | Production-level vs import-level 闭环语义契约 | sub-PR 8a 5-07 verify finding drift catch case #6, "完整闭环" claim sediment 时必显式标 import-level vs production-level | candidate | audit Week 2 batch (governance 沿用) |
| 5 | News API key SSOT (settings vs os.environ 选型) | sub-PR 8a 5-07 sediment, 本 PR 加入 5 News API key 走 settings (Pydantic) 反 os.environ 直读 | candidate | 已落实 sub-PR 8a (本身 ADR-DRAFT row 1 promote candidate, 0 ADR # reserve) |
| 6 | LiteLLM cost registry V4 gap (cost_usd=None 真生产风险 + 7-24 deadline plan) | sub-PR 8a-followup-B-yaml PR #247 5-07 sediment. LiteLLM model_cost **0 entry** for `deepseek/deepseek-v4-flash` + `deepseek/deepseek-v4-pro` → BudgetGuard cost_usd_total 永 0, V3 §20.1 #6 $50/月 budget cap **反 trigger**. 7-24 deadline: deepseek-chat / deepseek-reasoner 弃用前 LiteLLM SDK 升级 prerequisite. 真生产 evidence: 5-07 sub-PR 8a-followup-B Phase 1 8 path 真测 cost_usd=None for v4-* | candidate | LiteLLM SDK 升级 verify v4-* registry entry 生效 时 promote ADR-038 + 真生产 cost data 沿用体例 |
| 7 | Audit failure path coverage S2.4 sub-task 起手 sediment | sub-PR 8a-followup-B-audit PR #248 5-07 sediment 部分 closed (BUG #2 + #3). Sprint 1 PR #224 **success-only audit** deferred S2.4+ — **失败 path** error_class hardcoded None **audit blind primary fail signal** 5-07 sub-PR 8a 真生产 6 row error_class=NULL 反 is_fallback=True. 已部分 closed: BUG #2 dynamic detect 4 case + BUG #3 try/except 包络 failure path audit row + re-raise. **残余 sub-task**: failure path **S2.4 完整 design** (含 retry policy / circuit breaker / DingTalk push 触发 LL_AUDIT_INSERT_FAILED 等). **5-07 sub-PR 8b-llm-audit-S2.4 partial promote**: retry policy **ADR-039** sediment ✅; **残余 sub-task**: circuit breaker (sub-PR 8b-resilience 待办) + DingTalk push (sub-PR 9 待办) | **→ ADR-039 (committed, partial)** | 沿用 sub-PR 8b-resilience + sub-PR 9 待办 完整 closure |
| 8 | DeepSeek API 3 层暗藏机制 (alias-pass-through + backend silent routing + LiteLLM cost registry gap) | sub-PR 8a-followup-B Phase 1 5-07 真测真值 sediment 候选 #12 + #13 + #14 drift catch case汇总. **3 层机制**: (a) alias-pass-through layer (DeepSeek API echoes caller-sent model name as response.model, 反 underlying provider/model name) / (b) backend silent routing layer (deepseek-chat / deepseek-reasoner **legacy alias** 走 V4 underlying via thinking on/off, **dual-mode model** 沿用官方 7-24 deprecation map) / (c) LiteLLM cost registry layer (v4-* **0 cost data** until SDK 升级). drift catch #14 sediment: vanilla LiteLLM call 漏 thinking 参数 → 默认 thinking enabled → CC 3 次 push back 误归因, user 第 7 push back catch correctly (web_fetch 官方 API docs verify 真值). **5-08 chunk C-ADR promote**: 3 层 watch SOP **ADR-040** sediment ✅; **残余 sub-task**: 7-24 deadline plan governance PR (audit Week 2 batch B 候选) | **→ ADR-040 (committed)** | chunk C-ADR (5-08) closure ✅ |
| 9 | yaml double-model sync governance (反 single-model drift 体例) | sub-PR 8a-followup-B-yaml PR #247 5-07 sediment 沿用 user 决议 #4 反留尾巴. yaml 修**双 model 同步切换** (deepseek-v4-flash + deepseek-v4-pro 全切 V4 underlying), 反**单 model 切换** governance 漂移加深 (e.g. flash 切 V4 + pro 沿用 reasoner **alias-underlying inconsistency**). prompt 真测决议规则: "双 model path 1+2 不同步 → STOP escalate user (反单 model 切换, governance 漂移加深)". 真生产 5-07 yaml 双 model 同步 + thinking enabled/disabled **align V3 §5.5 chat/reasoner semantic** 体例. **5-08 chunk C-ADR promote**: 双 model sync governance **ADR-041** sediment ✅; **残余 sub-task**: 7-24 deadline plan migration governance PR | **→ ADR-041 (committed)** | chunk C-ADR (5-08) closure ✅ |
| 10 | vanilla LiteLLM call thinking 参数 verify SOP (web_fetch 官方文档 prerequisite) | sub-PR 8a-followup-B Phase 1 5-07 **drift catch #14** sediment. CC vanilla `litellm.completion(model=...)` **0 thinking 参数** → DeepSeek 默认 thinking enabled → reasoning_content 出现 → CC 3 次 push back 误归因 "silent routing reasoner", user 第 7 push back catch + 决议 web_fetch DeepSeek 官方 API docs 真测真值 (api-docs.deepseek.com/zh-cn/). SOP sediment: **任 3rd-party API frame finding/修复必 web_fetch 官方文档 verify prerequisite** (反 vanilla SDK call 默认参数误归因). 沿用 ADR-037 §Context 第 7 漂移类型 candidate (3rd-party API 默认参数误归因 silent semantic drift). **5-08 chunk C-ADR promote**: 3rd-party API spec watch SOP **ADR-042** sediment ✅; **残余 sub-task**: LL-109/110 sediment 加 LESSONS_LEARNED.md (chunk C-LL 真预约) | **→ ADR-042 (committed)** | chunk C-ADR (5-08) closure ✅ |
| 11 | D1 O8 automation-level persistence (singleton `pipeline_settings` table + GET/PUT `/api/pipeline/automation-level`) | iter 10 Inner-loop-B sediment 2026-05-23 (L4+R autonomous loop): design `docs/design/PN_001_automation_level_persistence.md` 完成. frontend `pipeline.ts:163 setAutomationLevel()` 已 wired 但 backend 0 endpoint (API_COVERAGE §6.1 O8 → 404). §7 重蹈 defense PASS (0 permanent-dead / conditional-fail; UI infra novel territory). §6 8-trigger STOP self-check 全 NEGATIVE (Feature 级). §4.5 value verdict IMPLEMENT (written justification: 小 scope + 0 红线触碰 + 0 架构风险 + unblocks PipelineConsole). 沿用 `strategy_registry` singleton table 体例 + `factors.py /{name}/archive` unguarded POST 体例 + `pipeline.py /trigger` `_require_local` 本机访问限制. | **→ ADR-087 (committed)** | PR #450 merged 2026-05-23 squash `d26ac2a`; ADR-087 row added to REGISTRY.md. Reviewer 0 P1 / 3 P2 accepted / 3 P3 accepted (commit `4822c43`); 6 backend pytest + 6 frontend vitest 全绿. |
| 12 | D1 O10 factor correlation prune (analysis-only `POST /api/factors/correlation-prune`, dry_run=True scope) | iter 11 Inner-loop-B sediment 2026-05-23 (L4+R autonomous loop, /loop self-paced): design `docs/design/PN_002_factor_correlation_prune.md` 完成 (commit `c4787c6`). frontend `factors.ts:206 triggerCorrelationPrune()` 已 wired 但 backend 0 endpoint (API_COVERAGE §6.1 O10 → 404); FactorLibrary.tsx:76 try/catch graceful fallback. §7 重蹈 defense PASS (0 permanent-dead / conditional-fail; UI factor pruning novel territory; LL hits 全是 RealtimeRisk CorrelatedDrop). §6 8-trigger STOP self-check 全 NEGATIVE (Feature 级 — analysis endpoint, 0 mining 方法 / 0 risk threshold / 0 DB mutation). §4.5 value verdict IMPLEMENT (复用 existing `factor_analyzer.factor_correlation_matrix` + `factors.py:148 GET /correlation` IC-series Spearman + CLAUDE.md doctrine `\|corr\| > 0.85` + IC 较低者 drop; 0 红线触碰; unblocks "相关性裁剪" UI button). | **→ ADR-088 (committed)** | PR #451 merged 2026-05-23/24 squash `f47a6df`; ADR-088 row added to REGISTRY.md. Reviewer 0 P1 / 0 P2 / APPROVE; 2 P3 accepted (sibling consistency + dead-code hoist, commit `88c49c7`); 5 backend pytest 全绿. |
| 13 | D1 O3 pipeline pause gate (`POST /api/pipeline/pause` + `/resume` + extend pipeline_settings singleton) | iter 12 Inner-loop-B sediment 2026-05-23/24 (L4+R autonomous loop, /loop self-paced): design `docs/design/PN_003_pipeline_pause_gate.md` 完成 (commit `9ee9468`). frontend `pipeline.ts:122 pausePipeline()` 已 wired 但 backend 0 endpoint (API_COVERAGE §6 O3 → 404); PipelineConsole.tsx:206 graceful catch. §7 重蹈 defense PASS (0 permanent-dead / conditional-fail; LL hits 都是 Beat schedule paused 7 天 / PR #150 link-pause 不同 scope). §6 8-trigger STOP self-check 全 NEGATIVE (Feature 级 — orchestration gate-at-entry, 0 mid-run abort / 0 broker / 0 .env). §4.5 value verdict IMPLEMENT (复用 PN-001 singleton + PN-002 mock-test 体例; storage: `paused_at TIMESTAMPTZ NULL` + `paused_reason TEXT NULL` ALTER ADD COLUMN; gate at `POST /trigger` (409 Conflict) + `run_gp_mining` / `run_bruteforce_mining` task body entry (skip + structured log); resume idempotent NULL set; 6 backend pytest + 3 frontend vitest). | **→ ADR-089 (committed)** | PR #452 merged 2026-05-23/24 squash `d406a8c`; ADR-089 row added to REGISTRY.md. Reviewer APPROVE; 0 P0 / 1 P1 declined (theoretical) / 4 P2 (2 accepted: typing + fail-safe doc, commit `94753c9`; 2 declined with reviewer concurrence) / 3 P3 declined. 11 backend pytest + 9 frontend vitest 全绿; smoke 61/61 全绿. |
| 14 | D1 `/api/pipeline/status` contract refactor (align response shape with frontend PipelineStatus type — 8 new derived/mapped fields + backward-compat aliases) | iter 15 Inner-loop-B sediment 2026-05-24 (L4+R autonomous loop, /loop self-paced): design `docs/design/PN_004_pipeline_status_contract_refactor.md` 完成 (commit `6936088`). API_COVERAGE §6.1 NEW finding (Phase K reconciliation 2026-05-22) surfaced contract-shape drift — backend returns 12 keys, frontend `PipelineStatus` expects 11 different/aliased keys; 5 fields currently `undefined` on frontend (FlowChart nodes / automation_level display / schedule_cron card / next-run / last-run). §7 重蹈 defense PASS (0 LL hit on status endpoint contract; novel territory). §6 8-trigger STOP self-check 全 NEGATIVE (orchestration only, read-only — 0 broker / 0 .env / 0 Beat schedule mutation). §4.5 value verdict IMPLEMENT (smallest remaining D1, finishes PN-001+PN-003 status work; 2 new helpers `_read_automation_level` + `_gp_weekly_schedule_next` stdlib-only ~12 lines + 4 unit tests; keep legacy keys as 1-sprint backward-compat aliases; 0 红线触碰). | **→ ADR-090 (committed)** | PR #453 merged 2026-05-24 squash `7218496`; ADR-090 row added to REGISTRY.md. Reviewer APPROVE; 0 P0 / 0 P1 / 2 P2 (1 accepted: last_run_at semantic docstring; 1 declined — math edge-case verified correct) / 3 P3 (1 accepted: datetime import hoist; 2 declined with reviewer concurrence). Fix commit `29e9fc1`. 15 backend pipeline pytest 全绿 (4 new + 6 pause + 5 automation-level); smoke 61/61 全绿. |
| 15 | Sprint 1.24 `generate_performance_report` Celery task + GET /api/reports/{sid}/latest (filesystem JSON artifact lifecycle, no new DB table) | iter 30 Inner-loop-B sediment 2026-05-24 (L4+R autonomous loop): closed `backend/app/api/report.py:198` Sprint 1.24 TODO. POST /api/reports/generate wired to real `.delay()` dispatch + AsyncResult.id (was random uuid4 stub) + NEW GET /{strategy_id}/latest endpoint reading `reports/{sid}_{date}_{mode}.json`. Filesystem JSON artifact pattern (atomic tmp-rename write; cleanup on failure; 0 new DB table by explicit scope decision). Sync psycopg2 task body via `get_sync_conn()` (sustained pattern from `daily_metrics_extract_tasks.py`). §6 8-trigger STOP self-check 全 NEGATIVE (Feature 级 — task layer + read-only DB). §7 重蹈 defense PASS (0 LL hit on report-generate). §4.5 value verdict IMPLEMENT (substantive bounded — closes real Sprint 1.24 TODO, 0 broker / 0 .env / 0 DDL). **Reviewer caught P0 ironlaw 25 violation**: SQL used `side`/`price` columns; actual `trade_log` schema = `direction`/`fill_price`. Fixed on branch via DDL fresh-read + added schema-aware EXPLAIN integration test (P0-2 mitigation for mock-only test pattern). | **→ ADR-091 (committed)** | PR #459 merged 2026-05-24 squash `34930c8`; ADR-091 row added to REGISTRY.md. Reviewer general-purpose Opus 4.7 fresh context (python-reviewer model-router gap fallback sustained from ADR-087/088/089/090). 1 P0 (SQL columns) + 1 P0-2 mitigation (schema-aware EXPLAIN integration test) + P1-3 key parity fix + 3 P2 (1 applied: P2-6 dead uuid import / 1 applied: P2-7 SIM117 nested-with / 1 applied: P2-8 retention TODO sediment) + 3 P3 declined with reviewer concurrence. Fix commit `144da79`. 17 mock + 1 schema-aware integration test 全绿; smoke 61/61 全绿. NEW LL "改什么读什么 extends to DDL canonical source — when modifying SQL, ALWAYS read DDL canonical (docs/QUANTMIND_V2_DDL_FINAL.sql), NOT just sustained neighbor callsite". |
| 16 | reports retention — `cleanup_old_reports` Celery task + Beat reports-cleanup-weekly Sunday 04:30 SH + `scripts/cleanup_old_reports.py` CLI (2-rule retention: age >90d OR count >20 per (sid, mode), union semantic) | iter 31 Inner-loop-B sediment 2026-05-24 (L4+R autonomous loop): closed iter 30 PR #459 reviewer P2-8 note (reports/ unbounded growth) AND **CRITICAL fix to iter 30 hidden defect**: `app.tasks.report_tasks` was NEVER registered in `celery_app.py` imports list — iter 30's `generate_performance_report.delay()` would dispatch but Celery worker never see task. Exact failure mode at `beat_schedule.py:408` sediment from Plan v10 P0-10+P0-16. Iter 31 caught + fixed + added regression-guard test `test_celery_imports_list_includes_report_tasks`. NEW Beat entry `reports-cleanup-weekly` Sunday 04:30 SH (collision-free with 03:00 VACUUM + 22:00 GP mining). CLI `scripts/cleanup_old_reports.py` mirrors task body (same 2-rule policy enforceable ad-hoc via `--dry-run`). §6 8-trigger STOP self-check 全 NEGATIVE (Feature 级 — FS-only ops). §7 重蹈 defense PASS (0 LL hit on reports retention). §4.5 value verdict IMPLEMENT (substantive bounded — closes iter 30 P2-8 + fixes iter 30 hidden defect; 0 broker / 0 .env / 0 DDL). | **→ ADR-092 (committed)** | PR #460 merged 2026-05-24 squash `6c878e8`; ADR-092 row added to REGISTRY.md. Reviewer general-purpose Opus 4.7 fresh context. APPROVE 0 P0/P1; 2 P2 applied pre-merge (P2-2 perf hoist + P3-3 vacuous crontab assertion strengthening); 3 P3 declined with reviewer concurrence. Fix commit `2628114`. 15 mock-based tests 全绿; smoke 61/61 全绿. **Post-merge X9 ops REQUIRED**: `Servy restart QuantMind-CeleryBeat AND QuantMind-Celery` (Beat schedule add + new task imports list entry both need worker restart per sustained pattern from S7/S8/regime/llm-cost/slippage entries). NEW LL "registration SSOT check before merge — any NEW Celery task module MUST verify celery_app.py imports list registration (iter 30 missed this; iter 31 caught + added regression-guard test)". |
| 17 | reports listing — `list_reports_for(sid, mode, limit)` helper + GET /api/reports/{sid}/list endpoint (historical reports enumeration with corrupt-JSON-included-with-marker pattern) | iter 32 Inner-loop-B sediment 2026-05-24 (L4+R autonomous loop): extends iter 30 single-fetch /latest with historical listing. NEW `list_reports_for(sid, mode=None, limit=20)` helper (filename regex parse, mtime DESC sort, limit cap, optional mode filter, summary preview extracted from JSON body, corrupt JSON entries INCLUDED with `_corrupt=True` marker rather than silently dropped per 铁律 33). NEW GET /api/reports/{strategy_id}/list endpoint (200 empty list / 200 multi-row / 400 bad mode). **Pivoted from iter 32 RESUME POINT cand (b) sensitivity analysis** after anti-assumption SOP verify: BacktestConfig lacks `cost_multiplier` override field + run_backtest reads config from stored row (no dispatch-time override) + each sub-backtest 30-60min + aggregation+shared-data-load is Architecture-level (§6 trigger 8 self-protection). Sensitivity = honest DEFER (paired with this PR per §5 same-commit-ship in row 18). §6 8-trigger STOP self-check 全 NEGATIVE (Feature 级 — helper + read-only endpoint). §7 重蹈 defense PASS. §4.5 value verdict IMPLEMENT (substantive bounded; mirrors iter 30 /latest pattern; complements iter 31 cleanup task — cleanup keeps latest 20, /list exposes the 20). | **→ ADR-093 (committed)** | PR #461 merged 2026-05-24 squash `8f02924`; ADR-093 row added to REGISTRY.md. Reviewer general-purpose Opus 4.7 fresh context. APPROVE 0 P0/P1; 1 P2 applied pre-merge (P2-1 safe_sid response symmetry doc clarification); 4 items declined with reviewer concurrence (P2-2 latest_report_path sustained sibling prefix-leak — inherited from iter 30, separate iter follow-up; P2-3 `_corrupt` key naming convention — sustained; P3-1 re.compile micro-perf; P3-2 mtime ISO test substring — structurally sound). Reviewer CONCUR with sensitivity DEFER verdict (verified `BacktestConfig.cost_multiplier` does NOT exist; existing `/cost-sensitivity` does linear extrapolation NOT sub-backtests). Fix commit `b603909`. 18 mock-based + 33 sustained tests 全绿; smoke 61/61 全绿. |
| 18 | backtest.py:1152 `/sensitivity` Celery sub-backtest dispatch — DEFER to Phase B post-PT-restart with Architecture-level scope sediment | iter 34 sediment 2026-05-24 (L4+R autonomous loop): closed iter 32 RESUME POINT cand (b) anti-assumption SOP verify → DEFER. Architectural gaps surfaced via fresh-verify: (i) BacktestConfig lacks `cost_multiplier` override field — config flows from stored backtest_run.config_json only, not dispatch arg; (ii) `run_backtest` Celery task signature is `run_backtest(self, run_id: str)` reads config from stored row (no dispatch-time override path); (iii) each sub-backtest is `soft_time_limit=3600` (1h) / `time_limit=3900` (~65min) — N sub-backtests for sensitivity analysis = N×30-60min runtime, naive impl wastes massive compute on redundant data load; (iv) aggregation + shared-data-load pattern + WebSocket pushback are Architecture-level decisions (§6 trigger 8 self-protection — affects mining queue + reporting + BacktestConfig schema). §6 STOP triggered cleanly (Architecture-level scope, NOT Feature). §4.5 written DEFER justification: (a) precondition gap 3-of-3 fails, (b) Architecture-level design needed (cost_multiplier whitelist + shared-data-load pattern + aggregation storage choice), (c) PT-paused state makes sensitivity-analysis low-priority research feature vs PT-restart prerequisites. **Paired with IMPLEMENT row 15/16/17 retroactive ADR sediment per §5 same-commit-ship**. | candidate (DEFER to Phase B post-PT-restart) | iter 34 PR ships sensitivity DEFER rationale block at `backend/app/api/backtest.py:1152` (replaces 1-line TODO with substantive 40+ line architectural-constraints + product-questions sediment). Future IMPLEMENT iter requires: ADR-XXX (cost_multiplier whitelist + BacktestConfig schema extension + sub-backtest shared-data-load pattern + WebSocket vs polling aggregation choice). Reviewer iter 32 CONCUR with DEFER verdict (verified `BacktestConfig.cost_multiplier` does NOT exist; existing `/cost-sensitivity` does linear extrapolation NOT sub-backtests). |

## maintenance 规则

### Append-only 意义

- 新 candidate 走**末尾 append**, 0 删 0 改顺序 (沿用 LL-099 append-only 体例).
- promote 时 row **保留** + `状态` 改 `committed` + `promote target` cite ADR-XXX.
- deprecated candidate **保留** + `状态` 改 `deprecated` + `promote target` cite 撤销原因.

### promote 走 SOP

新 candidate promote committed 时:
1. grep 4 source cross-verify (LL-105 SOP-6): V3 §18.1 / audit docs / sprint_state cite / LL backlog
2. 任一 source 漂移 → STOP + 反问 user
3. 全 source 一致 → reserve ADR-XXX in [REGISTRY.md](REGISTRY.md) → create ADR-XXX file + 同 PR update REGISTRY + ADR-DRAFT row 标 `→ ADR-XXX (committed)`

### deprecated 走 SOP

candidate **不再相关** (e.g. 上游设计变化 / source 撤销):
- row 保留 + `状态` 改 `deprecated` + `promote target` cite 撤销原因
- 反**物理删除** (沿用 append-only 体例, 历史可追溯)

## drift catch case candidate (本 file 触发)

### 候选 #7 (5-07 sediment, sub-PR 8a 触发)

**漂移类型**: ADR # reservation source 分散 (V3 §18.1 / audit docs / sprint_state cite / LL backlog 4 source 0 single source of truth, 5-02 sprint period 2 次 N×N 同步漂移 textbook 案例 — ADR-024 + ADR-027).

**case**: 5-06 ADR-037 + 铁律 45 SOP enforcement 后, 5-07 sub-PR 8a **首次 SOP**生效 catch P0 finding (Risk Beat 4-29 PAUSE 7 天 + Sprint 2 0 caller wire). 沿用 5-06 SOP **生效** 但 ADR # reservation 4 source SOP-6 **未生效** — 本 sub-PR 8a 触发 5 candidate (News retention / Query strategy SSOT / Beat indefinite paused / production-level 闭环 / News API key SSOT) **散落 LL backlog + sprint_state cite + V3 line cite**, **单 source 锁定** 候选 = 本 ADR-DRAFT.md.

**讽刺点**: ADR-DRAFT.md **预防 SOP-6 漂移** 的 cumulative sediment file, 但本 file **第一次 create 走 sub-PR 8a 5-07** — 沿用 5-02 sprint period sediment 后**5 天 0 触发**, sub-PR 8a **首次 catch** + create. 沿用 5-06 ADR-037 governance enforcement 体例.

**沿用 ADR-037 §Context 5 漂移类型 + 候选第 6 类**:
- **ADR # reservation source 分散** (本 file 预防类型) — 沿用 SOP-6 LL-105 sediment

## 引用规范

- 新引用: `docs/adr/ADR-DRAFT.md row N` (e.g. `ADR-DRAFT.md row 1` for News retention)
- promote 后引用: `ADR-XXX (committed)` + 沿用 [REGISTRY.md](REGISTRY.md) **SSOT cite** 体例
